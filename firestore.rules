rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /scores/{gameId}/leaderboard/{userId} {
      allow read: if true;
      // Scores are written only through Cloud Functions (Admin SDK).
      allow write: if false;
    }
  }
}
