/**
 * ThemeToggle — gaming temasında dark mode varsayılan olduğu için
 * bu bileşen artık minimal. İleride light mode eklenirse genişletilebilir.
 */
const ThemeToggle = () => {
  // Dark-only tema, toggle gizli
  return null;
};

export default ThemeToggle;
