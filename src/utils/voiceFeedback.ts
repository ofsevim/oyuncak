// Voice feedback utility (Speech synthesis disabled)

export const speak = (_text: string, _lang: string = 'tr-TR') => {
  // Speech synthesis disabled
};

export const speakSuccess = () => {
  // Speech synthesis disabled
};

export const speakTryAgain = () => {
  // Speech synthesis disabled
};

export const speakWelcome = () => {
  // Speech synthesis disabled
};

export const speakInstruction = (_text: string) => {
  // Speech synthesis disabled
};
